{
  "name": "@ps3d/exact-kernel-worker",
  "version": "0.1.0-contract.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/packages/exact-kernel-worker/tests/index.test.js && node -e \"require('node:fs').rmSync('.test-dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@ps3d/exact-kernel-api": "workspace:*"
  }
}
